import { SettingsPaneDirective } from './settings-pane.directive';

describe('SettingPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new SettingsPaneDirective();
    expect(directive).toBeTruthy();
  });
});
