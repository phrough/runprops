import { SettingPaneDirective } from './setting-pane.directive';

describe('SettingPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new SettingPaneDirective();
    expect(directive).toBeTruthy();
  });
});
